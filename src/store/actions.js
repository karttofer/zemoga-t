// Dependencies
import * as constants from '../commons/constants/contants';

// Action to add the elements
export const addElement = {
  type: constants.ADD_ELEMENTS,
  payload: {
    items: [],
  },
};
